# Next step 备忘

> 2026-08-26 复核：删除已完成、被后续方案替代或已冻结的事项。本文只保留仍需推进或验收的工作；实现细节以 `PROJECT_INDEX.md` 和各模块实现说明为准。

当前顺序：内容分级三轴展示已完成并进入维护；下一主线先做课程闭环观测和课件质量，其余能力保持后置。

## 内容分级与公开门禁（已完成，维护中）

- **优先级**：维护。
- **当前状态**：共享领域层、管理审核 API、公开三轴 DTO、推荐/排名/SEO/AI 过滤、阶段 1/2 迁移、后台审核页和运维脚本均已落地；公开三轴展示已开启，发布门禁仍保持关闭。
- **当前开关**：`public_v1_enabled=true`、`enforcement_enabled=false`。已复核内容在课程、项目、挑战卡片和详情页展示「适龄 / 难度 / 成人支持」三轴；未复核内容继续沿用原有可见性，但不输出三轴。
- **线上预检（2026-08-26）**：251 条内容中 219 条已发布，`published_unreviewed=0`、`published_incomplete=0`、非法内部星级为 0。152 条安全关键词只是规则提示，不再作为未完成事项，后续仅在内容变更或规则调整时辅助复核。

### 维护事项

1. **内容变更复核**：标题、正文、材料、步骤、课时或难度发生影响分级的变化后，继续由失效触发器让内容回到待复核，再通过 `/admin/content-classifications` 确认。
2. **运维脚本按需使用**：`preflight`、候选生成和审核报告只在内容批量变更、规则调整或发布前核查时手动运行，不接入页面请求、开发启动或每次构建。
3. **发布门禁后置**：如果未来要阻止未复核内容发布，再单独执行带 advisory lock 的 `enable_content_classification_rollout`；当前不改变既有发布流程。

详细 runbook：[`CONTENT_CLASSIFICATION_IMPLEMENTATION.md`](./CONTENT_CLASSIFICATION_IMPLEMENTATION.md)。

## 大颗粒课件批量上线：人工质量收尾

- **优先级**：P2。
- **已完成**：3 门年龄段课程与 300 个 `building_3d` 课时的 MVP 已上线；300/300 课件、PDF、成品图已上传，292/300 课有动画。批量管线和 OSS 核对脚本已稳定。

### 仍需推进

1. **人工样板课**：挑选少量代表课，逐页重写占位 `steps`，补齐材料清单和教学顺序，先建立可复用的审核标准。
2. **LDraw 审核**：`scripts/ldraw-models/` 中约 240 份批量稿仍按不可信草稿处理；当前 `public/courses/ldraw/` 已有 44 份 MPD，但文件存在不等于审核合格。必须人工核对零件、颜色、方向、连接、悬空/穿模和 BOM 后，才允许打包、迁移和上线。
3. **补齐动画**：处理 8 个源素材缺失的 `animation.mp4`；找回源文件，或确认无动画后清除对应 `videoUrl`，不要保留失效占位链接。

## 积木课程：完成闭环的回归与观测

- **优先级**：P2。
- **当前状态**：课时完成、课程进度展示、可信完成来源、审核通过作品奖励、整课 STEAM 里程碑、雷达过滤和 Admin 配置入口均已实现；课程迁移已执行，回归矩阵与只读完整性审计也已落地。课时不再发 `+15 XP`，作品 `+20 XP` 仅在审核通过后幂等发放。
- **实现说明**：[`COURSE_PROGRESS_REWARDS_AND_STEAM_IMPLEMENTATION.md`](./COURSE_PROGRESS_REWARDS_AND_STEAM_IMPLEMENTATION.md)。

### 已落地

1. **回归矩阵**：Vitest 覆盖原子完成 API、重复完成/审核、Scratch legacy 校验、匿名/登录课程列表与详情、空课程、课程增删课时后的当前进度、历史进度和完成反馈。
2. **只读审计入口**：`pnpm course:audit` 检查迁移、课程配置/约束、可信完成与里程碑缺口、重复/错误/孤立 XP、迁移后的 `complete_lesson` 流水和数据库死锁；`--json` 供外部采集，`--strict` 把 P2 advisory 也作为失败处理。
3. **线上基线（2026-08-26）**：默认审计通过；缺失奖励、重复奖励、非法/重复里程碑、可信完成缺里程碑、非法课程配置、未验证约束、关键索引缺失、迁移后新增 `complete_lesson` XP 和数据库死锁均为 0。另有 3 条历史 `complete_project` XP 找不到当前作品来源，已作为 P2 advisory 保留，不自动删除或冲正。

### 仍需推进

1. **定期告警**：将 `pnpm course:audit -- --json` 接入定时任务或发布后核对，并对 P1 失败、奖励/里程碑缺口和 reconcile 延迟发送告警；当前脚本仍是手动/外部调度入口。
2. **人工课件收尾**：继续按上方大颗粒课件清单审核样板课、LDraw 模型和缺失动画；这些质量判断不能由数据库审计替代。

## AI Tutor Agent（后置能力）

- **优先级**：P2，可在主线稳定后推进。
- Scratch 积木定位、自检/截图诊断、扫雷公开状态提示、PBL 当前阶段聚焦和工具选择评测已经落地。

### 可选后续

1. **迷宫下一步提示**：在不改变 BFS / DFS / A* 回放教学目标的前提下，增加小迪对当前可行下一格的受控高亮。
2. **技能池运营**：按 `xxx-skill` 规范沉淀更多场景能力，再评估 Admin 启停范围和代币消耗配置。

## 自然观察：新专题数据供给（后置）

- **优先级**：P3。
- **真菌专题**：目前仍是预告；补齐稳定的真菌物种数据后，再开放提交、鉴定和点亮进度。
- **矿物岩石 / 云类型**：先不进入当前开发，只有在需求重新确认且能维护有限类型清单时再评估。

## 现阶段不推进

- 游乐场现有联网对战和竞速房间保持维护，不扩展 2048、数独、迷宫等新的共享题面，也不做大厅/观战功能。
- 地域化、季节活动、观察手册、赛季经济、金币和会员产品继续冻结，等积木课程主线有真实使用数据后再重新评估。
